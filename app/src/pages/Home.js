import React from 'react';
import { Text } from '@rneui/themed';
import { DividerLayout } from '../components';

const Home = ({ navigation }) => {
  return (
    <DividerLayout>
      <Text>Home</Text>
    </DividerLayout>
  );
};

export default Home;
