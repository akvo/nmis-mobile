import React from 'react';
import { render } from '@testing-library/react-native';
import LoadingDialog from '../LoadingDialog';

describe('LoadingDialog', () => {
  test.todo('should render the LoadingDialog with the correct props and styles');

  test.todo('should show the loadingText prop in the dialog');

  test.todo('should be hidden when isVisible prop is false');

  test.todo('should be visible when isVisible prop is true');
});
