import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  fieldLabel: {
    marginTop: 10,
    marginLeft: 10,
    marginBottom: 5,
    fontWeight: 600,
    fontSize: 14,
  },
});
