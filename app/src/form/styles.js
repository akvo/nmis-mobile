import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  formContainer: {
    flex: 1,
  },
  fieldLabel: {
    marginTop: 10,
    marginLeft: 10,
    marginBottom: 5,
    fontWeight: 600,
    fontSize: 14,
  },
});
