export const UIState = {
  update: jest.fn(),
};

export const UserState = {
  update: jest.fn(),
};

export const AuthState = {
  update: jest.fn(),
};

export const FormState = {
  update: jest.fn(),
  useState: jest.fn(),
};
